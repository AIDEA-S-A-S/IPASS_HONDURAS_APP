import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    width: '48%',
    borderRadius: 10,
    padding: 10,
    marginVertical: 10,
    justifyContent: 'space-between'
    // marginHorizontal: 5
  }
})
